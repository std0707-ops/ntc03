GitHub Pages 使用方式

1. 解壓縮 ZIP。
2. 將 index.html、assets 資料夾與 .nojekyll 上傳至 GitHub 儲存庫根目錄。
3. 開啟 GitHub 儲存庫的 Settings → Pages。
4. 在 Build and deployment 選擇 Deploy from a branch。
5. 選擇 main 分支與 / (root)，按下 Save。

此網站為純靜態檔案，不需要 npm、Node.js 或建置指令。
